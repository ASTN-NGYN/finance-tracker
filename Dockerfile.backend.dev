# Use Maven with JDK 17
FROM maven:3.9.3-eclipse-temurin-17

WORKDIR /app

# Copy root pom.xml and download dependencies
COPY pom.xml .
RUN mvn dependency:go-offline

# Expose backend port
EXPOSE 8080

# Run Spring Boot in dev mode (requires spring-boot-devtools)
CMD ["mvn", "spring-boot:run"]
